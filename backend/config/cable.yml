development:
  url: <%= ENV.fetch("BACKEND_URL", "ws://localhost:3000/cable") %>
  adapter: async

test:
  adapter: test

production:
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>
  channel_prefix: backend_production
